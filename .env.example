# Server-side secrets (never commit real values)
TAPTOOLS_API_KEY=

# Client-side defaults (safe to expose)
NEXT_PUBLIC_DEFAULT_STAKE_ADDRESS=

# Optional: if you later proxy the T backend here
# T_BACKEND_API_KEY=
# T_BACKEND_BASE_URL=https://api-v2.fluxpointstudios.com